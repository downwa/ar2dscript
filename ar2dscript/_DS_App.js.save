/** DroidScript App emulation **/

module.exports = (_app) => {
    var _remote=require('./_rmt.js')(_app);
    var _rmt = _remote._rmt;
    return {
//////////////////////////////

SetOrientation: ( orient,callback ) => {
    // orient = Portrait, Landscape, or Default (not implemented yet)
    console.log("SetOrientation orient="+JSON.stringify(orient)+";callback="+JSON.stringify(callback)+";alen="+this.arguments.length);
    console.log("a0="+JSON.stringify(arguments[2]));
    console.log("a1="+arguments[3]);
    console.log("a2="+arguments[5]);
    return _rmt(function(orient,callback) {
	screen.orientation.lock(orient.toLowerCase()).then(callback, null); 
    }, arguments);
}

//////////////////////////////
    };
};
